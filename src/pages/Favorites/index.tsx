import { FavoritesFeed } from './favoritesComp';
import { Layout } from '../../common'

const Favorites = () => {
     return (
          <Layout>
               <FavoritesFeed />
          </Layout>
     )
}

export default Favorites